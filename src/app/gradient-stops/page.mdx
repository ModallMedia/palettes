import { ColorHarmonyDemo } from "@/components/ui/color/colorHarmonyDemo";
import ColorPickerAndDisplay from '@/components/ui/color/colorPickerAndDisplay'

import { Heading } from '@/components/Heading'
import {GradientColorDemo} from "@/components/ui/color/gradientColorDemo";

export const metadata = {
  title: 'Palettes • The Color Palette & Naming API Documentation',
  description:
    'Welcome to Palettes, the ultimate tool for all your color manipulation and palette generation needs. The API provides a comprehensive suite of functions for designers, developers, and color enthusiasts.',
}


export const sections = [
  { title: "Making a Request", id: "making-a-request",  tag: "GET"  },
  { title: "Gradient Stops Demo", id: "demo" },
];

## Gradient Stops API Endpoints
This API endpoint allows you to get gradients from an input color

<Note>
  If you appreciate our API and would like to support us, consider buying our book. It's a great way to deepen your understanding while helping us continue providing quality services. You can find the book at [our book page](/book). Your support is greatly appreciated!
</Note>

---

<Heading
  level={2}
  id='making-a-request'
  tag={"GET"}
  label={"/api/v1/gradient-stops"}
>
  Making A Request
</Heading>
<Row>
  <Col>
    <Properties>
      <Property name="color" type="string">
        A color to be used for the base of the color scheme.
      </Property>
      <Property name="count" type="number">
        The amount of colors to be used in the gradient
      </Property>

      <div className="not-prose py-6 flex gap-3">
        <Button href="/request-params" arrow="right">
          <>Read how these params work</>
        </Button>
      </div>
    </Properties>

  </Col>
  <Col sticky>

<CodeGroup title="Making a request" tag="GET" label="/api/v1/gradient-stops">
  ```bash
  curl "https://palettespro.com/api/v1/gradient-stops?color=<INPUT_COLOR>&count=<NUMBER_OF_COLORS_IN_GRADIENT>"
  ```
  ```js
// JavaScript (using fetch in a browser environment)
async function requestColorSchemes() {

  const color = "#ff0000"; // Replace with your color value
  const count = 5  // Replace with your count value

  const url = encodeUriComponent(`https://palettespro.com/api/v1/gradient-stops?color=${color}&count=${count}`);

  const response = await fetch(url);

  const data = await response.json();

  return data
}

```

```python
# Python (using requests library)
import requests

def request_color_schemes():
    color = "#ff0000"  # Replace with your color value
    count = 5  # Replace with your count value
    url = f"https://palettespro.com/api/v1/gradient-stops?color={color}&count={count}"
    response = requests.get(url)
    data = response.json()
    return data

# Call the function
color_schemes = request_color_schemes()
print(color_schemes)

````

```php
// PHP (using cURL)
function requestColorSchemes() {
    $color = "#ff0000";  // Replace with your color value
    $count = 5;  // Replace with your count value
    $url = "https://palettespro.com/api/v1/gradient-stops?color=" . urlencode($color) . "&count=" . urlencode($count);

    $curl = curl_init($url);
    curl_setopt($curl, CURLOPT_RETURNTRANSFER, true);

    $response = curl_exec($curl);
    if ($response === false) {
        curl_close($curl);
        return null;
    }

    curl_close($curl);
    return json_decode($response);
}

// Call the function
$colorSchemes = requestColorSchemes();
print_r($colorSchemes);
```

```ruby
# Ruby (using net/http)
require 'net/http'
require 'json'
require 'uri'

def request_color_schemes
  color = "#ff0000"  # Replace with your color value
  count = 5  # Replace with your count value
  url = URI("https://palettespro.com/api/v1/gradient-stops?color=#{URI.encode_www_form_component(color)}&count=#{count}")

  response = Net::HTTP.get(url)
  JSON.parse(response)
end

# Call the function
color_schemes = request_color_schemes
puts color_schemes
```

</CodeGroup>
```json {{ 'title': 'Example Response' }}
{
  "to_light": [
    {
      "opacity": "20%",
      "hex": "#e2edfd"
    },
    {
      "opacity": "40%",
      "hex": "#c5dafc"
    },
    {
      "opacity": "60%",
      "hex": "#a8c8fa"
    },
    {
      "opacity": "80%",
      "hex": "#8bb5f9"
    },
    {
      "opacity": "100%",
      "hex": "#6ea3f7"
    }
  ],
  "to_dark": [
    {
      "opacity": "0%",
      "hex": "#6ea3f7"
    },
    {
      "opacity": "20%",
      "hex": "#5882c6"
    },
    {
      "opacity": "40%",
      "hex": "#426294"
    },
    {
      "opacity": "60%",
      "hex": "#2c4163"
    },
    {
      "opacity": "80%",
      "hex": "#162131"
    }
  ],
  "from_light_to_dark": [
    {
      "opacity": "0%",
      "hex": "#ffffff"
    },
    {
      "opacity": "50%",
      "hex": "#b7d1fb"
    },
    {
      "opacity": "100%",
      "hex": "#6ea3f7"
    },
    {
      "opacity": "0%",
      "hex": "#6ea3f7"
    },
    {
      "opacity": "50%",
      "hex": "#37527c"
    },
    {
      "opacity": "100%",
      "hex": "#000000"
    }
  ],
  "gradient": [
    {
      "hex": "#9ec2fa",
      "isParam": false
    },
    {
      "hex": "#6ea3f7",
      "isParam": true
    },
    {
      "hex": "#0b51c1",
      "isParam": false
    },
    {
      "hex": "#052961",
      "isParam": false
    },
    {
      "hex": "#000000",
      "isParam": false
    }
  ]
}
```


```json {{ 'title': 'Example Error Response' }}
{
  "error": "Invalid color HEX code."
}
    ```
  </Col>
</Row>

---
<Heading level={2} id="demo">
  Gradient Stops Demo
</Heading>

<GradientColorDemo />